"""Adaptation of a pretrained multilingual model to Kabyle (Phase 11).

`docs/llm_adaptation_design.md` holds the measured architecture of the base model, the
memory arithmetic that decides what is trainable, and the two design choices the
literature refutes.
"""
